apiVersion: apps/v1
kind: Deployment
metadata:
  name: postprocess
  labels:
    app: postprocess
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: postprocess
  template:
    metadata:
      labels:
        app: postprocess
    spec:
      containers:
      - name: postprocess
        image: 192.168.100.3:5000/postprocess:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: SERVICE_NAME
          value: "postprocess"
        - name: MY_NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        resources:
          requests:
            cpu: "200m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: postprocess
spec:
  selector:
    app: postprocess
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP
